// Especifica tu conexión a la base de datos
datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

// Generador del cliente Prisma
generator client {
  provider = "prisma-client-js"
}

// Modelo de ejemplo (Product)
model Product {
  id          Int      @id @default(autoincrement())
  name        String
  price       Float
  description String?
  category    String
  createdAt   DateTime @default(now())
}